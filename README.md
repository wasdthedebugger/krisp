# Assessment

## Feedback Email Collection

The `Feedback_Email_Collection` folder contains the code for automating feedback emails to a spreadsheet.

## Object-Oriented Programming (OOP) Implementation

The `index.js` file inside `OOP` folder contains the implementation of the `User`, `Call`, and `CallManager` classes.
